/**
 * @file
 * th_tennet behaviors.
 */
(function (Drupal) {

  'use strict';

  Drupal.behaviors.thTennet = {
    attach: function (context, settings) {

      console.log('It works!');

    }
  };

} (Drupal));
